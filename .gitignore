node_modules/
.next/
out/
.DS_Store
Thumbs.db
tsconfig.tsbuildinfo

# Clone do three.js (referência de estudo, não faz parte do app)
three.js/

# Claude Code local settings (contains secrets)
.claude/settings.local.json

# Variáveis de ambiente locais: VENDA_TOKEN e META_CAPI_ACCESS_TOKEN vivem
# aqui para rodar as rotas em localhost. NUNCA versionar.
# .env.example fica de fora do ignore de propósito: serve de modelo, sem valor.
.env
.env.local
.env*.local

# Exports crus da Mixpanel (scripts/mixpanel-funil.mjs): trazem dados de
# visitantes reais, incluindo cidade e URL. Ficam na máquina, não no git.
mixpanel-*.json

.vercel
